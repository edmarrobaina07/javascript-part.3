

const users = { name: 'Edmar', age: 38, street: "Rua da Felicidade"} // objetos

for (const key in users) {
    console.log(`${key} : ${users[key]}`)    
    }

    // users.name
    // users['name']
